[build]
  publish = "dist"
  command = "npm ci --prefer-offline --no-audit && npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"

[cache]
  paths = ["node_modules/**", ".yarn/cache/**"]